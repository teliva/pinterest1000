# Notes

We will create a DB that a content server will populate and connect to the guid based on filters.
One is category ->
Two is style -> 
Three is room type -> 

For now an image can have only one explicit type
